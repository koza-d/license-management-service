spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: info # 실행되는 SQL 출력
    org.hibernate.orm.jdbc.bind: info # 바인딩 파라미터 값 출력
